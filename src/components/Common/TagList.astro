---
interface Props {
  tags?: string[]
}

const { tags = [] } = Astro.props
---

<div class="flex flex-row flex-wrap w-full gap-2">
  {
    tags.length &&
      tags.map((tag) => <span class="py-1 px-2 bg-red-200/30">{tag}</span>)
  }
</div>
